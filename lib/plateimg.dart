import 'package:flutter/material.dart';
import 'package:project/tools.dart';

class plateimg extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image(
        image: AssetImage(pizza4),
      ),
    );
  }
}
